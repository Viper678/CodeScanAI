# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/

# Node
node_modules/
.next/
dist/
build/
coverage/
*.tsbuildinfo
.pnpm-store/

# IDE
.idea/
.vscode/
*.swp

# Data (created at runtime by the app)
data/

# Playwright (T5.5 e2e suite)
codescan-frontend/playwright-report/
codescan-frontend/test-results/

# Local-only k8s manifests with environment-specific values (project ids,
# bucket names, image refs). These are generated on-demand from the
# placeholder manifests in deploy/k8s/ via sed substitution at test-deploy
# time; keep them off git so per-environment GCP details don't leak.
deploy/k8s/test/
