# Placeholder so codescan-frontend/public always exists in git. Next.js standalone's
# multi-stage Docker build copies this directory unconditionally
# (codescan-frontend/Dockerfile) — without this file the dir would be missing and
# the ``COPY ... ./public`` step would fail. Add static assets here when
# we have any.
